VERDICT: SHIP

| severity | file:line | issue | exploit | fix |
|---|---|---|---|---|
| CLEAN | routes/order_routes.py:528 | GATE BYPASS: `enforce_live_readiness=true` explicitly returns enforce; `false` only sets `wants_bypass`. | No metadata-only bypass found. Without both server flag and merchant allowlist, resolver falls through to `return True` at routes/order_routes.py:553. | None. |
| CLEAN | routes/order_routes.py:502 | BOOL PARSING: bools and strings `"true"/"false"/"0"/"off"` coerce as expected; `None`, missing, numeric `0`, and junk fail closed. | `enforce_live_readiness="false"` requests bypass, but still needs the env flag + allowlisted merchant. No wrong-way flip to bypass. | None. |
| CLEAN | routes/order_routes.py:515 | ENV FLAG: default is off; only `1,true,yes,on` after trim/lower enable the probe. | Unset, empty, `"0"`, `"off"`, `"false"` do not enable bypass. | None. |
| CLEAN | routes/order_routes.py:520 | MERCHANT MATCH: allowlist parsing strips/lowercases tokens and filters empty entries. | Empty/None merchant normalizes to `""`; empty tokens are not in the allowlist set, so `""` cannot match. No wildcard/near-match path found. | None. |
| CLEAN | routes/order_routes.py:550 | MERCHANT MATCH: request merchant is stripped/lowercased before membership check. | Only a request whose actual `merchant_id` matches an operator allowlist entry can bypass; metadata cannot supply a separate merchant. | None. |
| CLEAN | routes/order_routes.py:4024 | CALL SITE: preflight preferred-PSP check passes `merchant_id=order_request.merchant_id`. | `_ensure_explicit_preferred_psp_available` only skips readiness at routes/order_routes.py:468 when the gated resolver returns `False`; metadata alone cannot drive that. | None. |
| CLEAN | routes/order_routes.py:4242 | CALL SITE: payment creation path also passes `merchant_id=order_request.merchant_id` and forwards the same resolved value to `create_payment_with_failover` at routes/order_routes.py:4302. | No second order-create path left calling the resolver without merchant scope. | None. |
| CLEAN | routes/agent_api.py:7934 | TRUST: agent-facing create checks `context.can_access_merchant(order_request.merchant_id)` before delegating to `create_new_order` at routes/agent_api.py:8455. | For the external `/agent/v1/orders/create` path, attacker needs an authorized/allowed merchant that is also operator-allowlisted for the probe. | None. |
| CLEAN | routes/agent_shop_gateway.py:11357 | GATEWAY FORWARDING: gateway forwards `order.metadata` and `order.merchant_id` separately into the upstream body at routes/agent_shop_gateway.py:11387. | Metadata is still external, but the new gate reads the separate order merchant, not a metadata merchant override. | None. |
| CLEAN | routes/order_routes.py:524 | BLAST RADIUS: `rg` shows this resolver is only called by the two order-create sites above. Other `enforce_live_readiness` uses are explicit route/service params, not this metadata resolver. | No unrelated payment-execution flow changed by this diff. | None. |
| CLEAN | tests/test_order_routes_psp_resolution.py:102 | TESTS: new tests assert ungated bypass requests enforce, flag+wrong merchant enforces, allowlisted+flag bypasses, explicit enforce wins, and flag off enforces. | Covers the core security behavior. | None. |
| P3 | tests/test_order_routes_psp_resolution.py:123 | TEST GAP: scoped-bypass parametrization omits `test_psp_surfaces`; no direct empty-allowlist or string-token truth table. | No current exploit found; implementation defaults closed. Gap could miss a future alias/parsing regression. | Add parameterized cases for all bypass keys, string bool tokens, empty/whitespace allowlists, and case/whitespace merchant IDs. |

Pytest not run in this workspace; backend deps are unavailable per review note.
