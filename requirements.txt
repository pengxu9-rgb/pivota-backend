fastapi==0.104.1
uvicorn[standard]
httpx<0.28
python-dotenv
stripe>=15,<16  # money path: bound the major so per-request stripe_version (SPT preview) cannot vanish
SQLAlchemy==1.4.52
greenlet
databases==0.7.0
pydantic
pydantic-settings
alembic      # optional for migrations later
aiosqlite    # required for sqlite async driver
PyJWT        # for JWT token handling
jinja2       # for HTML templates
websockets   # for WebSocket support
aiohttp      # for async HTTP requests in admin routes
email-validator  # for email validation in auth routes
supabase>=2.0.0  # for Supabase integration
asyncpg      # for PostgreSQL async connections (Supabase)
psycopg2-binary  # for PostgreSQL synchronous connections (Supabase)
python-multipart  # for form data handling
pypdf>=4.0  # lab-report (PDF) text extraction for evidence intake (pure-Python)
cryptography  # for JWT and security
requests  # for HTTP requests (backup to httpx)
google-auth  # ADC/Workload Identity token minting for Vertex AI (services/vertex_gemini.py)
bcrypt
prometheus-client
boto3
Pillow
# PINNED, and the pin is load-bearing rather than hygiene. services/audit_scheduler.py's
# shutdown drain exists BECAUSE AsyncIOExecutor.shutdown ignores its `wait` argument and cancels
# every pending future regardless ("there is no way to honor wait=True without converting this
# method into a coroutine method", apscheduler 3.11.3). A test greps that source as a tripwire,
# so an unpinned bump turns a dependency change into a red CI whose message talks about design.
# A 4.x upgrade moves the module path entirely and means re-deriving stop_scheduler, not
# adjusting the test.
apscheduler==3.11.3  # PR-1b: weekly/monthly scheduled re-audit cron
python-pptx==1.0.2
