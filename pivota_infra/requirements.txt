fastapi==0.104.1
uvicorn[standard]
httpx
python-dotenv
stripe
SQLAlchemy
Databases
pydantic
pydantic-settings
alembic      # optional for migrations later
aiosqlite    # required for sqlite async driver
PyJWT        # for JWT token handling
jinja2       # for HTML templates
websockets   # for WebSocket support
aiohttp      # for async HTTP requests in admin routes
email-validator  # for email validation in auth routes
supabase>=2.0.0  # for Supabase integration
asyncpg      # for PostgreSQL async connections (Supabase)
psycopg2-binary  # for PostgreSQL synchronous connections (Supabase)
python-multipart  # for form data handling
cryptography  # for JWT and security
requests  # for HTTP requests (backup to httpx)
boto3  # for S3-compatible storage (Cloudflare R2)
